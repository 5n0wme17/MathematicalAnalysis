# 第七章 - 函数序列与函数级数

## 7.1 定义（逐点收敛）
**原文**：设{fₙ}, n=1,2,3,...是定义在集合E上的函数序列，假设对每个x∈E，数列{fₙ(x)}收敛。我们定义函数f为f(x)=lim_{n→∞} fₙ(x) (x∈E)。此时称{fₙ}在E上逐点收敛于f。

## 7.2 例子
**原文**：展示逐点收敛但不一致收敛的例子。

## 7.3 例子
**原文**：展示连续函数序列逐点收敛于不连续函数的例子。

## 7.4 例子
**原文**：展示函数序列fₙ(x)=xⁿ在[0,1]上的收敛性。

## 7.6 例子
**原文**：展示函数序列fₙ(x)=nxe^{-nx²}的收敛性。

## 7.7 定义（一致收敛）
**原文**：我们称函数序列{fₙ}, n=1,2,3,...在E上一致收敛于函数f，如果对每个ε>0，存在整数N使得对所有n≥N和所有x∈E，有|fₙ(x)-f(x)|<ε。

## 7.8 定理（一致收敛的柯西准则）
**原文**：定义在E上的函数序列{fₙ}在E上一致收敛当且仅当对每个ε>0，存在整数N使得当m,n≥N时，对所有x∈E有|fₙ(x)-fₘ(x)|<ε。

## 7.9 定理（一致收敛的极限运算）
**原文**：设fₙ→f一致于E，且每个fₙ在E上有界。则f在E上有界。

## 7.10 定理（一致收敛与连续性）
**原文**：设{fₙ}是定义在E上的函数序列，设x是E的极限点。假设lim_{t→x} fₙ(t)=Aₙ (n=1,2,3,...)，且fₙ→f一致于E。则{Aₙ}收敛，且lim_{t→x} f(t)=lim_{n→∞} Aₙ。

## 7.11 定理（一致收敛保持连续性）
**原文**：如果{fₙ}是E上的连续函数序列，且fₙ→f一致于E，则f在E上连续。

## 7.12 定理（紧致集上的一致收敛）
**原文**：假设K紧致，且：
1. {fₙ}是K上连续函数序列
2. {fₙ}在K上逐点收敛于连续函数f
3. fₙ(x)≥fₙ₊₁(x)对所有x∈K和n=1,2,3,...成立
则fₙ→f一致于K。

## 7.13 定义（有界连续函数空间）
**原文**：如果X是度量空间，ℬ(X)表示定义域为X的所有复值、连续、有界函数的集合。

## 7.14 定理（ℬ(X)的完备性）
**原文**：上述度量使ℬ(X)成为完备度量空间。

## 7.15 定理（一致收敛与积分）
**原文**：设α在[a,b]上单调递增。假设fₙ∈ℛ(α)在[a,b]上，n=1,2,3,...，且假设fₙ→f一致于[a,b]。则f∈ℛ(α)在[a,b]上，且∫_a^b f dα=lim_{n→∞} ∫_a^b fₙ dα。

## 7.16 定理（一致收敛与导数）
**原文**：假设{fₙ}是在[a,b]上可导的函数序列，且对某个点x₀∈[a,b]有{fₙ(x₀)}收敛。如果{fₙ'}在[a,b]上一致收敛，则{fₙ}在[a,b]上一致收敛于某个函数f，且f'(x)=lim_{n→∞} fₙ'(x) (a≤x≤b)。

## 7.17 定理（无处可导的连续函数）
**原文**：存在实直线上的实连续函数，处处不可导。

## 7.18 定义（逐点有界）
**原文**：设{fₙ}是定义在集合E上的函数序列。如果对每个x∈E，数列{fₙ(x)}有界，则称{fₙ}在E上逐点有界。

## 7.19 例子
**原文**：展示逐点有界但不一致有界的例子。

## 7.20 例子
**原文**：展示等度连续性的例子。

## 7.21 定义（等度连续）
**原文**：定义在度量空间X的子集E上的复函数族ℱ称为在E上等度连续，如果对每个ε>0，存在δ>0使得对所有f∈ℱ和所有满足d(x,y)<δ的x,y∈E，有|f(x)-f(y)|<ε。

## 7.22 定理（逐点有界序列的子序列收敛）
**原文**：如果{fₙ}是可数集E上的逐点有界复函数序列，则{fₙ}有子序列{fₙₖ}使得对每个x∈E，{fₙₖ(x)}收敛。

## 7.23 定理（紧致集上的一致有界性）
**原文**：如果K是紧致度量空间，fₙ∈ℬ(K)对n=1,2,3,...，且{fₙ}在K上一致收敛，则{fₙ}在K上等度连续。

## 7.24 定理（阿尔泽拉-阿斯科利定理）
**原文**：如果K紧致，fₙ∈ℬ(K)对n=1,2,3,...，且{fₙ}在K上逐点有界且等度连续，则{fₙ}在K上有一致收敛的子序列。

## 7.25 定理（魏尔斯特拉斯逼近定理）
**原文**：如果f是[a,b]上的连续复函数，则存在多项式序列Pₙ使得lim_{n→∞} Pₙ(x)=f(x)一致于[a,b]。

## 7.26 推论
**原文**：对每个区间[-a,a]，存在实多项式序列Pₙ使得Pₙ(0)=0且lim_{n→∞} Pₙ(x)=|x|一致于[-a,a]。

## 7.27 定义（代数）
**原文**：定义在集合E上的复函数族𝒜称为代数，如果(i) f+g∈𝒜，(ii) fg∈𝒜，且(iii) cf∈𝒜对所有f,g∈𝒜和所有复数c成立。

## 7.28 定理（一致闭包）
**原文**：设𝒜是有界函数代数𝒜的一致闭包。则𝒜是一致闭代数。

## 7.29 定义（分离点）
**原文**：设𝒜是集合E上的函数族。如果对E中每对不同的点x₁,x₂，存在函数f∈𝒜使得f(x₁)≠f(x₂)，则称𝒜在E上分离点。

## 7.30 定理（分离点引理）
**原文**：假设𝒜是集合E上的函数代数，𝒜在E上分离点，且𝒜在E的每点不消失。假设x₁,x₂是E中不同的点，c₁,c₂是复数。则存在f∈𝒜使得f(x₁)=c₁，f(x₂)=c₂。

## 7.31 定理（斯通-魏尔斯特拉斯定理，实情形）
**原文**：设𝒜是紧致集K上的实连续函数代数。如果𝒜在K上分离点且𝒜在K的每点不消失，则𝒜的一致闭包由K上所有实连续函数组成。

## 7.32 定理（斯通-魏尔斯特拉斯定理，复情形）
**原文**：假设𝒜是紧致集K上的自伴复连续函数代数，𝒜在K上分离点，且𝒜在K的每点不消失。则𝒜的一致闭包由K上所有复连续函数组成。

*注：第七章包含更多小节和详细内容，此处为简要概述。*
